# Prompt Quizzer – MCQ Quiz (10 Questions)

## 🧠 Web Dev I – Lab Assignment 4

This project is a Multiple-Choice Quiz Game created using **pure JavaScript**.  
It runs **entirely inside the browser console** using `prompt()` for questions and `alert()` for feedback.

No HTML or CSS is required.

---

## 🎯 Objective

- Use JavaScript arrays and objects
- Work with functions and loops
- Take user input using `prompt()`
- Provide output using `alert()`
- Normalize user input using `trim()` and `toLowerCase()`
- Track and display score and percentage

---

## 🚀 Features

✔️ 10 MCQ questions  
✔️ 3 options (A/B/C) per question  
✔️ Score tracking  
✔️ Final percentage displayed  
✔️ Runs only in browser console  
✔️ Input validation (`a`, `b`, `c`)  

---

## 📂 Project Files

- `quiz.js` — contains full JavaScript quiz logic  
- `README.md` — instructions and documentation  
- `screenshots/` — contains 7 screenshots
- `index.html` - to link quiz.js file

---

## 💻 How to Run the Quiz (Chrome)

1. Open Google Chrome  
2. Press **F12** to open Developer Tools  
3. Click the **Console** tab  
4. Open `quiz.js` and **copy all the code**  
5. Paste it into the console  
6. Press **Enter**  
7. In the console, type:

   ```js
   runQuiz();
